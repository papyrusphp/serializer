grumphp:
    tasks:
        phpstan:
            configuration: phpstan.neon
        phpcsfixer:
            config: .php-cs-fixer.php
            verbose: true
            allow_risky: true
        phpunit:
            config_file: phpunit.xml
